package com.example.cipher_events.user;

/**
 * Represents the entrant's status for a given event.
 */
public enum Status {
    WAITLISTED,
    SELECTED,
    NOT_SELECTED,
    REGISTERED,
    CANCELLED
}